extends layout

block content

  table(class="table table-striped table-sm")
    tbody
      each build, i in builds
        tr
          td
            a(href=`/history/buildDetails?buildID=` + build.build_id)= build.owner
          td
            a(href=`/history/buildDetails?buildID=` + build.build_id)= build.repository
          td
            a(href=`/history/buildDetails?buildID=` + build.build_id)= build.build_key
          td
            a(href=`/history/buildDetails?buildID=` + build.build_id)= build.build
          td= build.status